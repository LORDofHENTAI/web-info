import { BarcodeImgPipe } from './barcode-img.pipe';

describe('BarcodeImgPipe', () => {
  it('create an instance', () => {
    const pipe = new BarcodeImgPipe();
    expect(pipe).toBeTruthy();
  });
});
