import { StrToDataPipe } from './str-to-data.pipe';

describe('StrToDataPipe', () => {
  it('create an instance', () => {
    const pipe = new StrToDataPipe();
    expect(pipe).toBeTruthy();
  });
});
